const initialState = {
  type: '' // Enemy, Boss
  life: '',
  attack: ''
}

export default (state = initialState, action)  => {
  switch(action.type)
    {

      default:       return state   // something
    }
}
